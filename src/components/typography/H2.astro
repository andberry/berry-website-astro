---
import type { HTMLTag } from "astro/types";
export interface Props {
  as?: HTMLTag;
  class?: string;
}
const { as: Tag = "h2", class: additionalClasses = "" } = Astro.props;
---

<Tag
  class:list={["font-bold text-clamph2 leading-[0.9] my-4", additionalClasses]}
>
  <slot />
</Tag>
