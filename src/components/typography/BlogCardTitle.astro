---
import type { HTMLTag } from "astro/types";
export interface Props {
  as?: HTMLTag;
  class?: string;
}
const { as: Tag = "h3", class: additionalClasses = "" } = Astro.props;
---

<Tag
  class:list={[
    "text-3xl leading-[1] mt-2 inline-block [text-wrap:balance] py-2",
    additionalClasses,
  ]}
>
  <slot />
</Tag>
