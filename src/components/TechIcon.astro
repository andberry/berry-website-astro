---
import { Icon } from "astro-icon";

export interface Props {
  icon: string;
  title: string;
}

const { icon, title } = Astro.props;
---

<div class="flex flex-col items-center justify-center">
  <Icon class="w-9" name={icon} text={title} />
  <div class="text-sm mt-2">{title}</div>
</div>
